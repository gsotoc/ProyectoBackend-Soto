<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="/styles/home.css">
</head>
<body>

  <div class="form-container">

    <h1>Login</h1>
    <form class="card-form" action="/api/sessions/login" method="POST">
      <div>
        <label for="email">Email:</label>
        <input 
          type="email" 
          name="email" 
          id="email" 
          required
        />
      </div>

      <div>
        <label for="password">Contraseña:</label>
        <input 
          type="password" 
          name="password" 
          id="password" 
          required
        />
      </div>

      <div>
        <p>Aún no tienes cuenta? <a href="/api/sessions/register"> Registrate aquí</a> </p>
      </div>

      <button type="submit">Iniciar sesión</button>
    </form>

    {{#if error}}
      <div class="error-msg">
        Credenciales incorrectas. Intente nuevamente.
      </div>
    {{/if}}
  </div>

  
</body>
</html>